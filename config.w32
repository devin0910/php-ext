// $Id$
// vim:ft=javascript

// If your extension references something external, use ARG_WITH
// ARG_WITH("ever", "for ever support", "no");

// Otherwise, use ARG_ENABLE
// ARG_ENABLE("ever", "enable ever support", "no");

if (PHP_EVER != "no") {
	EXTENSION("ever", "ever.c");
}

